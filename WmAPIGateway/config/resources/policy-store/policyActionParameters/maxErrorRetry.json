  {
		"templateKey" : "maxErrorRetry",
		"names" : [
		   {
			 "value" : "Maximum no of Error Retry",
			 "locale" : "en"
		   }
		 ],
		 "descriptions" : [
		   {
			 "value" : "The maximum number of times that a retryable failed request (ex: a 5xx response from a service) will be retried. Or -1 if the user has not explicitly set this value, in which case the configured RetryPolicy will be used to control the retry count.",
			 "locale" : "en"
		   }
		 ],
		"type" : "INT",
		"isRequired" :"false",
		"isArray" : "false"
  }